{% extends 'base.html.twig' %}

{% block title %}Hello FriendsController!{% endblock %}
    {% block stylesheets %}
        {{ encore_entry_link_tags('friendsstyle') }}
    {% endblock %}
    {% block javascripts %}
        {{ encore_entry_script_tags('messageDisplay') }}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

{% block body %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
    {% include 'navbar/index.html.twig' %}
    <div class="container-fluid d-flex justify-content-start align-items-center border border-2 text-white">
        Inbox
        Send
        <div class="container d-flex justify-content-start align-items-start w-25 bg-danger">
            test
        </div>
        <div class="container-fluid justify-content-start align-items-start w-25 column text-nowrap lh-1">
            test
        </div>
    </div>

    <div class="border-end border-2 w-25 text-white p-3 bg-black vh-100">
        {% for messages in message %}
            <div class="message" data-user="{{ messages|serialize(format ='json') }}">
                <h4>{{ messages.fromUser }}</h4>
                <h6>tytuł =>{{ messages.title }}</h6>
                <h6>opis => {{ messages.description }}</h6>
            </div>
        {% endfor %}
    </div>
    <div class="container-fluid position-absolute text-white border border-2 w-75 vh-100"
         style="left: 315px; top: 110px;">
        <div id="test" class="d-flex  h-100 ">
            <p>From: <h6 class=""></h6></p>
            <p>Title:<h4></h4></p>
            <p>Description:<h5></h5></p>

        </div>
    </div>



{% endblock %}

